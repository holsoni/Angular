import { HoursPipePipe } from './hours-pipe.pipe';

describe('HoursPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HoursPipePipe();
    expect(pipe).toBeTruthy();
  });
});
